# Installing Node.js

Node.js installation is simple–I followed the [AdaFruit tutorial](https://learn.adafruit.com/node-embedded-development/installing-node-dot-js):

```sh
curl -sLS https://apt.adafruit.com/add | sudo bash
sudo apt-get install node
```

Now you should have Node.js and NPM installed:

```sh
$ npm --version
2.11.2
$ node --version
v0.12.6
```
